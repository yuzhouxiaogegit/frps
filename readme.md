# frp 服务端一键安装脚本
### 支持系统 CentOS 7/8, Debian, Ubuntu (Systemd)

```code
wget https://raw.githubusercontent.com/yuzhouxiaogegit/frps/main/frps_install.sh && chmod +x frps_install.sh
./frps_install.sh
```
